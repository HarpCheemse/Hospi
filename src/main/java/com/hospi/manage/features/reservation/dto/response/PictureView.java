package com.hospi.manage.features.reservation.dto.response;

/** Lightweight view model for a room type picture ID, safe for JavaScript serialization. */
public record PictureView(
        Long id
) {}
