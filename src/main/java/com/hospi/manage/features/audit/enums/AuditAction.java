package com.hospi.manage.features.audit.enums;

public enum AuditAction {
    ASSIGN_ROOM,
    CHANGE_PASSWORD,
    CHECKIN,
    CHECKOUT,
    CLEAN_ROOM,
    CREATE,
    DELETE,
    DIRTY_ROOM,
    EXTEND_STAY,
    MAINTENANCE_ROOM,
    PAYMENT,
    REMOVE_ROOM,
    SWAP_ROOM,
    UPDATE
}
