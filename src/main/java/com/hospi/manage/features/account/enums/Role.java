package com.hospi.manage.features.account.enums;

/**
 * Staff roles used for URL-based authorization in the security filter chain.
 */
public enum Role {
    ADMIN,
    LEADER,
    RECEPTIONIST,
    MANAGER
}
